{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Resume Analyzer (Frontend & Backend)",
      "request": "launch",
      "type": "node",
      "cwd": "${workspaceFolder}",
      "runtimeExecutable": "npm",
      "runtimeArgs": ["run", "dummy"],
      "preLaunchTask": "Run Both Servers",
      "console": "integratedTerminal"
    }
  ]
}
